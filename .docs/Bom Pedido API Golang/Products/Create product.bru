meta {
  name: Create product
  type: http
  seq: 1
}

post {
  url: {{API_BASE_URL}}/v1/products
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ADMIN_TOKEN}}
}

body:json {
  {
    "name": "Guarana",
    "price": 4.90
  }
}
