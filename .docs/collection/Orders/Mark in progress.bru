meta {
  name: Mark in progress
  type: http
  seq: 4
}

post {
  url: {{API_BASE_URL}}/v1/orders/:id/in-progress
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ADMIN_TOKEN}}
}

params:path {
  id: {{ORDER_ID}}
}
